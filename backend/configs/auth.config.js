//secret Property: This is the secret key used to sign and verify JWTs. 
//When creating a JWT, this secret is used to sign the token, and when validating a received JWT, it's used to verify the token's authenticity.

module.exports = {
	secret: "Vyshakh-Babu-secret-key",
};
